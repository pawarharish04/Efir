services:
  backend:
    build: .
    ports:
      - "10000:10000"
    environment:
      MONGO_URI: mongodb://mongo:27017/fir
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
